FROM debian:buster
MAINTAINER gipark <gipark@student.42seoul.kr>

#Package update && Install utils
RUN apt-get update && apt-get install -y vim unzip curl

#Install nginx && Setting nginx
RUN apt-get install -y nginx
